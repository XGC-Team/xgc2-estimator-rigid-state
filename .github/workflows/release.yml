name: release

on:
  workflow_dispatch:
    inputs:
      expected_version:
        description: Expected .xgc2/product.yml version
        required: true
        type: string
      expected_source_sha:
        description: Expected source commit SHA
        required: true
        type: string
      publish_apt:
        description: Publish built debs to the XGC2 APT repository
        required: true
        default: false
        type: boolean
      run_cpp_quality:
        description: Run clang-format and clang-tidy checks
        required: true
        default: false
        type: boolean
      run_source_tests:
        description: Run source-tree catkin tests before Docker deb builds
        required: true
        default: false
        type: boolean

defaults:
  run:
    shell: bash

jobs:
  validate-release-inputs:
    name: Validate release inputs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Validate expected version and source SHA
        run: |
          set -euo pipefail
          actual_version="$(awk -F': *' '/^version:[[:space:]]*/ {print $2; exit}' .xgc2/product.yml)"
          actual_sha="$(git rev-parse HEAD)"
          test "${actual_version}" = "${{ inputs.expected_version }}"
          test "${actual_sha}" = "${{ inputs.expected_source_sha }}"

  cpp-quality:
    name: C++ quality
    needs: validate-release-inputs
    runs-on: ubuntu-latest
    container: ros:noetic-ros-base-focal
    steps:
      - name: Skip C++ quality
        if: ${{ !inputs.run_cpp_quality }}
        run: echo "C++ quality disabled by input"

      - uses: actions/checkout@v4
        if: inputs.run_cpp_quality

      - name: Install quality dependencies
        if: inputs.run_cpp_quality
        run: |
          export DEBIAN_FRONTEND=noninteractive
          apt-get update
          apt-get install -y --no-install-recommends ca-certificates
          echo "deb [trusted=yes arch=$(dpkg --print-architecture)] https://xgc2.apt.xiaokang.ink focal main" \
            > /etc/apt/sources.list.d/xgc2.list
          apt-get update
          apt-get install -y --no-install-recommends \
            build-essential \
            clang-format \
            clang-tidy \
            cmake \
            git \
            libxgc2-math-dev \
            libxgc2-state-machine-dev \
            rsync \
            ros-noetic-geometry-msgs \
            ros-noetic-message-generation \
            ros-noetic-roscpp \
            ros-noetic-xgc2-ros1-utils \
            ros-noetic-sensor-msgs \
            ros-noetic-std-msgs

      - name: Run C++ quality gate
        if: inputs.run_cpp_quality
        run: .xgc2/scripts/check_cpp_quality.sh

  source-tests:
    name: Source build and tests
    needs: validate-release-inputs
    runs-on: ubuntu-latest
    container: ros:noetic-ros-base-focal
    steps:
      - name: Skip source tests
        if: ${{ !inputs.run_source_tests }}
        run: echo "Source tests disabled by input"

      - uses: actions/checkout@v4
        if: inputs.run_source_tests
        with:
          fetch-depth: 2

      - name: Install build dependencies
        if: inputs.run_source_tests
        run: |
          export DEBIAN_FRONTEND=noninteractive
          apt-get update
          apt-get install -y --no-install-recommends ca-certificates
          echo "deb [trusted=yes arch=$(dpkg --print-architecture)] https://xgc2.apt.xiaokang.ink focal main" \
            > /etc/apt/sources.list.d/xgc2.list
          apt-get update
          apt-get install -y --no-install-recommends \
            build-essential \
            cmake \
            git \
            libxgc2-math-dev \
            libxgc2-state-machine-dev \
            rsync \
            ros-noetic-geometry-msgs \
            ros-noetic-message-generation \
            ros-noetic-roscpp \
            ros-noetic-roslaunch \
            ros-noetic-rospack \
            ros-noetic-xgc2-ros1-utils \
            ros-noetic-sensor-msgs \
            ros-noetic-std-msgs

      - name: Check package metadata
        if: inputs.run_source_tests
        run: git diff --check HEAD

      - name: Build package and run launch compliance
        if: inputs.run_source_tests
        run: |
          set -euo pipefail
          work_dir="${RUNNER_TEMP}/estimator-rigid-state-compliance"
          install_root="${RUNNER_TEMP}/estimator-rigid-state-install-root"
          rm -rf "$work_dir" "$install_root"
          mkdir -p "$work_dir/src/estimator-rigid-state"
          rsync -a --delete "$GITHUB_WORKSPACE/" "$work_dir/src/estimator-rigid-state/"

          cd "$work_dir"
          set +u
          source /opt/ros/noetic/setup.bash
          set -u
          catkin_make run_tests_estimator_vrpn_px4_rotor_state run_tests_estimator_vrpn_ugv_state
          catkin_test_results
          DESTDIR="$install_root" catkin_make install \
            -DCMAKE_INSTALL_PREFIX=/opt/ros/noetic \
            -DCMAKE_BUILD_TYPE=Release
          set +u
          source devel/setup.bash
          set -u
          test "$(rospack find estimator_vrpn_px4_rotor_state)" = "$work_dir/src/estimator-rigid-state/estimator_vrpn_px4_rotor_state"
          test "$(rospack find estimator_vrpn_ugv_state)" = "$work_dir/src/estimator-rigid-state/estimator_vrpn_ugv_state"
          roslaunch --files estimator_vrpn_px4_rotor_state vrpn_px4_rotor_state_estimator.launch >/tmp/xgc2-vrpn-px4-rotor-state-estimator-files.txt
          roslaunch --files estimator_vrpn_ugv_state vrpn_ugv_state_estimator.launch >/tmp/xgc2-vrpn-ugv-state-estimator-files.txt

  build-debs:
    name: Build debs (${{ matrix.arch }})
    needs:
      - validate-release-inputs
      - cpp-quality
      - source-tests
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-latest
          - arch: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - name: Build and install-check deb package in Docker
        run: |
          .xgc2/scripts/build_debs_in_docker.sh \
            --work-dir "$RUNNER_TEMP/estimator-rigid-state-${{ matrix.arch }}" \
            --output-dir "$GITHUB_WORKSPACE/debs"

      - name: Upload deb artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xgc2-estimator-rigid-state-debs-${{ matrix.arch }}
          path: debs/*.deb

  publish-apt:
    name: Publish APT
    if: inputs.publish_apt
    needs: build-debs
    runs-on: ubuntu-latest
    env:
      APT_REPO_HOST: ${{ secrets.APT_REPO_HOST }}
      APT_REPO_PORT: ${{ secrets.APT_REPO_PORT }}
      APT_REPO_SSH_KEY: ${{ secrets.APT_REPO_SSH_KEY }}
      APT_REPO_KNOWN_HOSTS: ${{ secrets.APT_REPO_KNOWN_HOSTS }}
    steps:
      - uses: actions/checkout@v4

      - name: Download deb artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: xgc2-estimator-rigid-state-debs-*
          path: debs
          merge-multiple: true

      - name: Publish to self-hosted APT repository
        if: ${{ env.APT_REPO_HOST != '' && env.APT_REPO_PORT != '' && env.APT_REPO_SSH_KEY != '' && env.APT_REPO_KNOWN_HOSTS != '' }}
        run: .xgc2/scripts/publish_apt_repo.sh --deb-dir debs
